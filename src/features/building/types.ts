/**
 * Building feature types.
 *
 * The live `Building` model now lives in `../production/types`. This file only
 * holds product-name identifiers that are referenced from the resources layer
 * and across the rest of the codebase.
 */

export enum ProductName {
  AMMO = "Ammo",
}
