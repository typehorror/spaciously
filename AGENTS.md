# Spaciously

Solo space-opera strategy game in the browser. Vite + React + TypeScript.

## Agent skills

### Issue tracker

GitHub issues at `typehorror/spaciously`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary — label strings equal their canonical role names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
